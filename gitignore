# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_meta

# Data (keep raw & processed folders tracked but ignore large files)
data/raw/*.sqlite
data/raw/*.db
*.csv.gz

# Models: .pkl files ARE committed so HF Spaces can run inference without retraining.
# Uncomment below only if you want to exclude them (and re-train on every deploy):
# models/*.pkl
# models/*.joblib

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
